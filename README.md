# Portfolio

This is my portfolio website created with nextjs

## Getting Started

First, run the development server and open [http://localhost:3000](http://localhost:3000) with your browser to see the result:

```bash
pnpm dev
```

Build the project with:

```bash
pnpm build
```

And serve the built static files with:

```bash
pnpm serve
```

## Future plans

- Implement blogs
- Use SSG
- analytics and insights
- og
- llms.txt

```html
<div
  style={{
    height: '100%',
    width: '100%',
    display: 'flex',
    flexDirection: 'column',
    backgroundColor: '#fff',
  }}
>
  <svg
    width="1200"
    height="630"
    fill="none"
    stroke="black"
  >
    <path d="M 10 10 V 620 H 1190 V 10 H 10 Z"></path>
    
  </svg>
  <div style={{ 
    marginRight: '30',
    alignItems: 'flex-end',
    justifyContent: 'flex-end', 
    fontSize: 100,
    fontWeight: 400,
    }}>mehulsinha.com</div>
</div>

```
